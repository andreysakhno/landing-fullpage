'use strict';

const { initMusic } = require('./music');
const { initSwiper } = require('./swiper');
const { initFullPage } = require('./full-page');

module.exports = {
  initFullPage,
  initSwiper,
  initMusic,
};
